//import { ADD_TODO, TOGGLE_TODO } from "../actionTypes";

const initialState = {label: "Et voici les notes de la journée, \\n avec un retrou chariot et tout !"};

export default function(state = initialState, action) {
  switch (action.type) {

    default:
      return state;
  }
}